version: '3.8'

services:
  api:
    build: ./server
    container_name: quoteforge-api
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=5000
      # Add other env vars here (e.g., JWT_SECRET)

  web:
    build: ./client
    container_name: quoteforge-web
    restart: always
    ports:
      - "80:80"
    depends_on:
      - api
